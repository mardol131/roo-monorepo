{
  "name": "@roo/scripts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "populate:regions": "tsx populate/regions/regions.ts",
    "populate:districts": "tsx populate/districts/districts.ts",
    "populate:cities": "tsx populate/cities/cities.ts",
    "populate:filters": "tsx populate/filters/filters.ts",
    "populate:specific": "tsx populate/specific/specific.ts",
    "populate:all": "pnpm run populate:regions && pnpm run populate:districts && pnpm run populate:cities && pnpm run populate:filters && pnpm run populate:specific"
  },
  "dependencies": {
    "@roo/common": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.20.3",
    "typescript": "^5.7.3"
  }
}
